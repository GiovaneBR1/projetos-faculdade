CRIAR O BANCO DE DADOS NO MYSQL

CREATE DATABASE IF NOT EXISTS sistema_login;
USE sistema_login;

CREATE TABLE usuarios (
  id INT AUTO_INCREMENT PRIMARY KEY,
  email VARCHAR(255) NOT NULL UNIQUE,
  senha_hash VARCHAR(255) NOT NULL,
  nome VARCHAR(255) NOT NULL,
  data_nascimento DATE
);

CREATE TABLE acesso (
  id INT AUTO_INCREMENT PRIMARY KEY,
  ativo BOOLEAN DEFAULT TRUE,
  tentativas_rest INT DEFAULT 5,
  id_usr INT NOT NULL,
  FOREIGN KEY (id_usr) REFERENCES usuarios(id) ON DELETE CASCADE
);


HELP -> ECLIPSE MARKETPLACE -> PESQUISAR = EFXCLIPSE 3.8.0(INSTALAR)


// AQUI PARA O CONFIGURAÇÃO DA VM. EM ARGUMENTS

RUN AS -> RUN CONFIGURATIONS ->ARGUMENTS -> VM ARGUMENTS

--module-path "CAMINHO DO JAVAFX" --add-modules javafx.controls,javafx.fxml